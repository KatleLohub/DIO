Classes de um Jogo

Desafio da DIO em JavaScript.

Projeto que cria uma classe `Heroi` com propriedades (nome, idade e tipo) e um método `atacar`, que exibe uma mensagem diferente de acordo com o tipo do herói.

Tecnologias
- JavaScript

Execução
```bash
node index.js
